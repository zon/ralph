You are an AI agent that generates an orchestration document.

Read the specification file at:
{{.SpecPath}}

Write an orchestration.md file in the same directory as the specification file, following the orchestration format document installed in the repository. If the repository has an installed orchestration-authoring skill, use that instead. If neither the format document nor the skill is present, stop and report that no orchestration format or skill is installed.
