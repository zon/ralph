Fix the project YAML at `{{.ProjectFile}}` so it parses and validates correctly.

## Load Error

{{.LoadError}}

Rewrite the file at `{{.ProjectFile}}` with corrected content so it can be parsed as valid YAML and passes project validation.