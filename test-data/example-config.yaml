# Example .ralph/config.yaml
maxIterations: 10
baseBranch: main

services:
  - name: example-service
    command: echo
    args: ["Running service..."]
    # port: 8080  # Optional: for health checking
