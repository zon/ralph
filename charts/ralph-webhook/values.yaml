namespace: ralph-webhook

replicaCount: 1

image:
  repository: ghcr.io/zon/ralph-webhook
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: public
  clusterIssuer: lets-encrypt
  annotations: {}
  hostname: ralph.wurbs.chat
  path: /
  pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

serviceAccount:
  name: ralph-webhook
