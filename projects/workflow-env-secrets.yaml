- The `workflow.env` section of `.ralph/config.yaml` accepts, per variable, either a literal string value or a `secretKeyRef` mapping with `name` and `key`, and both forms coexist in the same map. A `secretKeyRef` entry missing `name` or `key` is rejected with a descriptive error when the config is loaded
- An env var configured with `secretKeyRef` renders into the submitted Argo Workflow's container env as a `valueFrom` with `secretKeyRef` pointing at the named secret and key, while literal-valued env vars keep rendering as plain `value` entries, and built-in env vars are unchanged
- Apply a semver minor bump to the app version
