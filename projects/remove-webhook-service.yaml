- "The ralph-webhook service application is removed: the `cmd/ralph-webhook` entrypoint, the `internal/webhook` HTTP server, the `internal/webhookconfig` configuration package, the `internal/orchestration/webhooksetconfig` orchestration, and the GitHub webhook registration and payload-parsing code in `internal/github` (`webhook.go`, `webhook_payload.go`, their tests, and the `RegisterWebhook` method on the GitHub client interface, implementation, and mock). The webhook-to-workflow builder `internal/workflow/from_event.go` and its tests are deleted. The repository builds and the full test suite passes."
- "The ralph workflow comment feature is removed: the `ralph workflow comment` subcommand and its help text, `internal/cmd/workflow_comment.go` and `comment_test.go`, the `Comment` field on the workflow command group, `internal/orchestration/comment`, the comment-triggered rendering in `internal/workflow` (the `CommentBody` and `PRNumber` fields, the comment branch in `workflow.go`, and the `comment-body` and `pr-number` workflow parameters and environment variables), the comment instructions in `internal/config` (`comment-instructions.md`, the `CommentInstructions` field, `DefaultCommentInstructions`, and the comment half of `loadInstructions`), and the `PostComment` method on the GitHub client. `ralph workflow comment` no longer appears in help output. The repository builds and the full test suite passes."
- "The ralph-webhook Helm chart and its build and versioning references are removed: `charts/ralph-webhook` is deleted, the chart assertions in `internal/version/version_test.go` are dropped, and the webhook build, install, and clean targets in `justfile` and the `ralph-webhook` copy step in `Containerfile` are removed. The repository builds and the full test suite passes."
- "The webhook and workflow-comment specs and documentation are removed: `specs/features/webhook`, `specs/features/ralph/workflow-comment`, the webhook-related module entries in `specs/architecture.yaml` (`cmd/ralph-webhook`, `internal/webhook`, `internal/webhookconfig`, `internal/orchestration/webhooksetconfig`, `internal/orchestration/comment`), the webhook and workflow-comment entries in `specs/README.md`, the webhook module lines in `plan.md`, and the webhook and chart references in the versioning and testing sections of `AGENTS.md` and `CLAUDE.md`. The repository builds and the full test suite passes."
- "Apply a semver major bump to the app version, since removing the webhook service, its chart, and the `ralph workflow comment` subcommand is a breaking change."
