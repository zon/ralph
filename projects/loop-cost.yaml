- When a `ralph loop` runs inside a workflow container (the default mode, via `ralph workflow loop`), the command prints accumulated AI token usage and cost statistics at the end of execution, regardless of whether the loop succeeded or failed, matching the behavior of `ralph run` described in [run-local/spec.md](../specs/features/ralph/run-local/spec.md). When the loop runs locally with `--local`, no token usage or cost statistics are printed.
- Apply a semver minor bump to the app version.
