- "`ralph run` accepts a `--namespace` flag alongside the existing `--context`. When the run submits a remote workflow (`--mode remote`), the flag overrides the workflow namespace from `.ralph/config.yaml` for the submission and for any followed logs, matching the shared kubectl targeting contract in `specs/features/ralph/kubectl/spec.md`. The flag appears in `ralph run --help`. The repository builds and the full test suite passes."
- "`ralph loop` accepts a `--namespace` flag alongside the existing `--context`. When the loop submits a remote workflow (`--mode remote`), the flag overrides the workflow namespace from `.ralph/config.yaml` for the submission and for any followed logs, matching the shared kubectl targeting contract in `specs/features/ralph/kubectl/spec.md`. The flag appears in `ralph loop --help`. The repository builds and the full test suite passes."
- "`ralph command` accepts `--context` and `--namespace` flags and uses them for the submitted workflow and its streamed logs, overriding the Kubernetes context and namespace from `.ralph/config.yaml`, matching the shared kubectl targeting contract in `specs/features/ralph/kubectl/spec.md`. The flags appear in `ralph command --help`. The repository builds and the full test suite passes."
- "The CLI reference in `docs/cli.md` lists `--namespace` for `ralph run` and `ralph loop`, and `--context` and `--namespace` for `ralph command`, matching the help output of each command. The repository builds and the full test suite passes."
- "Apply a semver minor bump to the app version, since the new `--namespace` and `--context` flags add backwards-compatible features."
